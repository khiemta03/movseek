import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['th.bing.com', 'www.kindpng.com', 'image.tmdb.org', 'ambigram.com.pl'], // Thêm domain của ảnh
  },
};

export default nextConfig;
